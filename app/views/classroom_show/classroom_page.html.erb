<% if page_owned_by_current_indexable? %>
  <% content_for :show_content do %>
    <ul class="unstyled">
      <%= render :partial => "enrolled_emails/enrolled_email", collection: @page_classroom.enrolled_emails.order("confirmed", "LOWER(name)") %>
    </ul>
  <% end %>
<% end %>

<%= render partial: "classroom_show/base_classroom_page", locals: { current_showing: :student_list }%>
